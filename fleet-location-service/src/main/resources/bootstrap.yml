server:
  port: 9000
spring:
  application:
    name: fleet-location-service
  jpa:
    hibernate:
      ddl-auto: update

---
spring:
  profiles: dev

security:
#  enabled: false
  basic:
    enabled: false

management:
  security:
    enabled: false